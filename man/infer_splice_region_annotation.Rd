% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infer_splice_region_annotation.R
\name{infer_splice_region_annotation}
\alias{infer_splice_region_annotation}
\title{Infer Splice Region Annotation}
\usage{
infer_splice_region_annotation(maf_data)
}
\arguments{
\item{maf_data}{A dataframe containing HGVSc and HGVSp_Short columns.}
}
\value{
A dataframe with updated HGVSp_Short labels based on splice region.
}
\description{
Extracts cDNA position and splice offset (e.g., c.123+1G>A) to calculate the
affected amino acid and assign a protein label (e.g., p.Splice_Donor+1@AA41).
}
\details{
The function uses ceiling(abs(cdna_pos)/3) to determine the Amino Acid position.
Labels are assigned as Splice_Donor (+), Splice_Acceptor (-), or NearSplice (no offset).
}
\examples{
input <- GAMBLR.open::get_coding_ssm()
input \%>\%
  select(HGVSp_Short) \%>\% 
  head(100) \%>\%
  as.data.frame()
result <- infer_splice_region_annotation(maf_data = input)
}
